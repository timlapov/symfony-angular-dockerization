<nav class="navbar navbar-expand-lg navbar-light fixed-top" style="background-color: rgba(255, 255, 255, 0.7); backdrop-filter: blur(10px);">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_index') }}">Dream Glimpse</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarScroll">
            <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_post_crud_index') }}">Articles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_user_list') }}">Utilisateurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_post_crud_new') }}">Ajouter un article</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_show', {'id': app.user.id}) }}">
                            Mon Compte
                        </a>
                    </li>
                {% endif %}
            </ul>
            {% if app.user %}
            <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary me-1">Déconnecter ({{ app.user.username }})</a>
            {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-outline-secondary me-1">Connecter</a>
            {% endif %}
        </div>
    </div>
</nav>